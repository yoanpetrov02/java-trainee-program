package com.sirma.javacourse.networking.calculator;

/**
 * Used to run the application without having to create module-info.java.
 * Otherwise, use mvn javafx:run to launch the app from the terminal.
 */
public class CalculatorMockRunner {

	public static void main(String[] args) {
		CalculatorApplication.main(args);
	}
}
